# second-task.yml
---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: busybox

inputs:
  - name: vars

#params:
#  PARAMS_FILE: params.yml

params:
  CAT_NAME:
  DOG_NAME:

run:
  path: sh
  args:
    - -exc
    - |
      echo "Job Starting"
      echo ${CAT_NAME}
      echo ${DOG_NAME}
      sleep 100
